<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'users/info', user: @book.user %>
      <%= render 'books/post', book: @book %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>
        <table>
          <tr>
            <th></th>
            <th>Title</th>
            <th>opinion</th>
          </tr>
          <tr>
            <td></td>
            <td>
              <%= @book.title %>
            </td>
            <td><%= @book.body %></td>
          </tr>
        </table>
    </div>
  </div>
</div>